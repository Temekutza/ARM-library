<Window x:Class="ARM_library.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Книга" Height="340" Width="480" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="HeaderText" Grid.Row="0" Grid.ColumnSpan="2" FontSize="16" FontWeight="SemiBold" Text="Новая книга" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Название:"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Grid.Column="1" Height="26"/>

        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0" Text="Автор:"/>
        <TextBox x:Name="AuthorBox" Grid.Row="2" Grid.Column="1" Height="26" Margin="0,10,0,0"/>

        <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0" Text="Издательство:"/>
        <TextBox x:Name="PublisherBox" Grid.Row="3" Grid.Column="1" Height="26" Margin="0,10,0,0"/>

        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0" Text="Год издания:"/>
        <TextBox x:Name="YearBox" Grid.Row="4" Grid.Column="1" Height="26" Margin="0,10,0,0" Width="80" HorizontalAlignment="Left"/>

        <TextBlock Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0" Text="Количество:"/>
        <TextBox x:Name="QuantityBox" Grid.Row="5" Grid.Column="1" Height="26" Margin="0,10,0,0" Width="80" HorizontalAlignment="Left" Text="1"/>

        <TextBlock x:Name="ErrorText" Grid.Row="6" Grid.ColumnSpan="2" Foreground="Firebrick" Margin="0,10,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="OkButton" Width="90" Margin="0,0,8,0" Content="Добавить" Click="Ok_Click"/>
            <Button Width="90" Content="Отмена" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
